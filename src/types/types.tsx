export type MovieLinkProp = {
  name: string;
};

export type NavbarLinkType = {
  label: string;
  link: string;
  end: boolean;
};

export type MovieLinkType = {
  title: string;
};

export type MovieType = {
  title: string;
};